{
  "name": "imx",
  "version": "1.0.0",
  "private": true,
  "main": "electron-main.js",
  "scripts": {
    "start": "bash start.sh",
    "dev": "echo Run dev:server and dev:web in separate terminals",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "test": "npm test --prefix server && npm test --prefix web",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix web",
    "build": "npm run build --prefix web && node scripts/copy-web-dist.mjs && cd server && npx prisma generate && npm run build",
    "build:prod": "npm run build --prefix web && node scripts/copy-web-dist.mjs && npm run build --prefix server",
    "electron:dev": "electron .",
    "electron:build": "electron-builder --win",
    "electron:build:portable": "electron-builder --win portable"
  },
  "build": {
    "appId": "com.imx.app",
    "productName": "IMX",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "electron-main.js"
    ],
    "win": {
      "target": [
        "nsis",
        "portable"
      ],
      "icon": "web/public/icon.png"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  },
  "devDependencies": {
    "electron": "^43.4.1",
    "electron-builder": "^26.15.3",
    "sharp": "^0.35.3"
  }
}
